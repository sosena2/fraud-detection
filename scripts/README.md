# Scripts

Use this folder for lightweight command-line entry points or automation helpers if you want to run the pipeline outside the notebooks.

Current core logic lives in `src/` and is meant to be imported by the notebooks.
